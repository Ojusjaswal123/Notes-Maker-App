<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="POST">
          <input
            class="block px-5 py-2 w-full rounded-md bg-zinc-800 outline-none"
            type="text"
            placeholder="Title goes here"
            name="title"
          />
          <textarea
            class="block px-5 py-2 mt-2 w-full rounded-md resize-none bg-zinc-800 focus:outline-none"
            name="details"
            id=""
            placeholder="write your tasks details here..."
          ></textarea>
          <input
            type="submit"
            value="create task"
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            type="submit"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach(function(val) { %>
        <div class="task minw-72 px-3 rounded-md py-4 bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
          <a class="text-blue-500 inline-block mt-3" href="/file/<%=val%>"
            >Read More</a
          >
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">no task yet</h3>
        <% } %>

        <!-- <div class="task w-72 px-3 rounded-md py-4 bg-zinc-800 hidden">
          <h1 class="text-white text-3xl tracking-tighter">
            Some task goes here...
          </h1>
          <a class="text-blue-500 inline-block mt-3" href="">Read More</a>
        </div> -->
        <!-- <div class="task w-72 px-3 rounded-md py-4 bg-zinc-800 hidden">
          <h1 class="text-white text-3xl tracking-tighter">
            Some task goes here...
          </h1>
          <a class="text-blue-500 inline-block mt-3" href="">Read More</a>
        </div>
        <div class="task w-72 px-3 rounded-md py-4 bg-zinc-800 hidden">
          <h1 class="text-white text-3xl tracking-tighter">
            Some task goes here...
          </h1>
          <a class="text-blue-500 inline-block mt-3" href="">Read More</a>
        </div> -->
      </div>
    </div>
  </body>
</html>
